name: CD

  on:
    push:
      branches: [main]

  jobs:
    deploy:
      runs-on: ubuntu-latest
      needs: []

      steps:
        - name: Trigger Render Deploy
          run: |
            curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"
